---
import { getProfile } from '../data/profile';
import { getExperience } from '../data/experience';
import { tools } from '../data/tools';
import { getCaseStudies } from '../data/caseStudies';
import { getWork } from '../data/work';
import {
  getRecommendations,
  featuredRecommendationIds,
} from '../data/recommendations';
import { faqs } from '../data/faqs';
import { Picture } from 'astro:assets';
import { getBlogPostHref, getBlogPosts, getBlogTagHref } from '../data/blog';
import { localePrefix, type Locale } from '../i18n/config';
import { t } from '../i18n/messages';
import HomepageConversion from './HomepageConversion.astro';
import ContactForm from './ContactForm.astro';
import { getHomepageConversionContent } from '../data/homepageConversion';
import {
  heroPortraitQuality,
  heroPortraitSizes,
  heroPortraitWidths,
} from '../data/images';
import portraitImage from '../../public/assets/images/portrait-1100.webp';
import '../styles/homepage.css';

const { locale = 'en' } = Astro.props as { locale?: Locale };
const profile = getProfile(locale);
const conversion = getHomepageConversionContent(locale);
const experience = getExperience(locale);
const work = getWork(locale);
const caseStudies = getCaseStudies(locale);
const featuredCaseStudy =
  caseStudies.find(
    (caseStudy) => caseStudy.slug === 'performance-modernization',
  ) ?? caseStudies[0];
const caseStudySlugs = new Set(caseStudies.map((caseStudy) => caseStudy.slug));
const recommendations = getRecommendations(locale);
const pageHref = (path: string) => `${localePrefix(locale)}${path}` || '/';
const featuredCaseStudyHref = pageHref(`/work/${featuredCaseStudy.slug}/`);
const linkedin = profile.social.find((item) => item.label === 'LinkedIn');
const heroStack = [
  'React',
  'TypeScript',
  'Redux Toolkit',
  'FastAPI',
  'PostgreSQL',
] as const;
const skillColumns = [
  {
    heading: t(locale, 'skills.frontendCore', 'Frontend Core'),
    items: [
      'React + Next.js',
      'TypeScript + JavaScript',
      'HTML5 + CSS3 + Sass',
      'Tailwind CSS',
    ],
  },
  {
    heading: t(locale, 'skills.stateData', 'State & Data'),
    items: [
      'Redux Toolkit + RTK Query',
      'TanStack Query',
      'React Hooks + Context API',
      'GraphQL',
    ],
  },
  {
    heading: t(locale, 'skills.uiLibraries', 'UI Libraries'),
    items: ['Ant Design', 'Chakra UI', 'Headless UI', 'Bootstrap 5'],
  },
  {
    heading: t(locale, 'skills.backendApis', 'Back-End & APIs'),
    items: [
      'FastAPI (Python)',
      'PostgreSQL',
      'MongoDB',
      'Node.js',
      'REST API design',
    ],
  },
] as const;
const recruiterHighlights = [
  {
    label: t(locale, 'home.recruiterRoleLabel', 'Role fit'),
    value: t(locale, 'home.recruiterRoleValue', 'Senior Front-End Engineer'),
  },
  {
    label: t(locale, 'home.recruiterProofLabel', 'Proof'),
    value: t(locale, 'home.recruiterProofValue', '6 case studies · 6+ years'),
  },
  {
    label: t(locale, 'home.recruiterBestForLabel', 'Best for'),
    value: t(
      locale,
      'home.recruiterBestForValue',
      'SaaS dashboards, React architecture, performance',
    ),
  },
] as const;
const heroProofCards = [
  {
    className: 'hero-proof-card hero-case-card hero-proof-card--work',
    eyebrow: t(locale, 'home.heroWorkEyebrow', 'Selected work'),
    title: featuredCaseStudy.title,
    body: t(
      locale,
      'home.heroWorkBody',
      'Confidential case studies show architecture, constraints, decisions, and outcomes without client-sensitive detail.',
    ),
    href: featuredCaseStudyHref,
    cta: t(locale, 'caseStudy.read', 'Read case study'),
    tags: featuredCaseStudy.tags.slice(0, 3),
  },
  {
    className: 'hero-proof-card hero-proof-card--stack',
    eyebrow: t(locale, 'home.heroStackEyebrow', 'Core stack'),
    title: t(locale, 'home.heroStackTitle', 'React + TypeScript systems'),
    body: t(
      locale,
      'home.heroStackBody',
      'Frontend architecture, state management, APIs, accessibility, design systems, and Core Web Vitals tuned for production.',
    ),
    href: '#expertise',
    cta: t(locale, 'home.heroStackCta', 'Explore expertise'),
  },
  {
    className: 'hero-proof-card hero-proof-card--availability avail-card',
    eyebrow: t(locale, 'home.availability', 'Availability'),
    title: t(
      locale,
      'home.availabilityLead',
      'Remote freelance and part-time contracts',
    ),
    body: t(
      locale,
      'home.availabilityMeta',
      '24–28 hours weekly · Available now · 2–4 hours overlap',
    ),
    href: '#contact',
    cta: t(locale, 'action.contact', 'Get in touch'),
  },
] as const;

const posts = await getBlogPosts(locale);
const articleIllustrations = [
  '/assets/illustrations/article-performance.svg',
  '/assets/illustrations/article-accessibility.svg',
  '/assets/illustrations/article-ui-engineering.svg',
] as const;
const featured = recommendations.filter((r) =>
  featuredRecommendationIds.includes(
    r.id as (typeof featuredRecommendationIds)[number],
  ),
);

// Converts "May 2025" → "2025-05" for <time datetime> semantic markup
const M: Record<string, string> = {
  Jan: '01',
  Feb: '02',
  Mar: '03',
  Apr: '04',
  May: '05',
  Jun: '06',
  Jul: '07',
  Aug: '08',
  Sep: '09',
  Oct: '10',
  Nov: '11',
  Dec: '12',
};
const toISO = (s: string) => {
  const [m, y] = s.split(' ');
  return y && M[m] ? `${y}-${M[m]}` : '';
};

// Category labels shown above each tool chip in the marquee
const toolCategories: Record<string, string> = {
  HTML5: 'LANGUAGE',
  TypeScript: 'LANGUAGE',
  React: 'FRONTEND',
  'Next.js': 'FRONTEND',
  Redux: 'STATE',
  'TanStack Query': 'STATE',
  GraphQL: 'BACKEND',
  'Node.js': 'RUNTIME',
  Figma: 'DESIGN',
  'Tailwind CSS': 'STYLING',
  'Bootstrap 5': 'STYLING',
  Git: 'DEVOPS',
  Sass: 'STYLING',
  jQuery: 'FRONTEND',
  'Claude AI': 'AI',
  Storybook: 'TESTING',
  Azure: 'CLOUD',
  FastAPI: 'BACKEND',
  PostgreSQL: 'DATABASE',
  MongoDB: 'DATABASE',
};
const toolCategoryLabel = (toolName: string) => {
  const category = toolCategories[toolName] ?? 'TOOL';
  return t(locale, `tool.category.${category.toLowerCase()}`, category);
};
---

<div class="homepage-shell">
  <!-- ── HERO BENTO ──────────────────────────────────── -->
  <section id="home" class="hero" aria-labelledby="hero-name">
    <div class="wrap">
      <div class="hero-grid">
        <div class="hero-content hero-card">
          <p class="hero-eyebrow">
            <span>{conversion.hero.eyebrow}</span>
            <img
              src="/assets/icons/india-flag-icon.svg"
              alt=""
              aria-hidden="true"
              width="16"
              height="12"
              loading="lazy"
              decoding="async"
            />
          </p>

          <h1 id="hero-name" class="hero-name">
            {
              locale === 'ar' ? (
                profile.name
              ) : (
                <>
                  Suprabhat <span>Kumar</span>
                </>
              )
            }
          </h1>
          <p class="hero-sub">{conversion.hero.title}</p>
          <p class="hero-lead">{conversion.hero.lead}</p>

          <dl
            class="hero-recruiter-list"
            aria-label={t(
              locale,
              'home.recruiterSnapshotAria',
              'Recruiter snapshot',
            )}
          >
            {
              recruiterHighlights.map((item) => (
                <div>
                  <dt>{item.label}</dt>
                  <dd>{item.value}</dd>
                </div>
              ))
            }
          </dl>

          <ul
            class="hero-stack-list"
            aria-label={t(locale, 'home.stackAria', 'Technology stack')}
          >
            {heroStack.map((item) => <li>{item}</li>)}
          </ul>

          <div class="hero-ctas">
            <a href="#work" class="btn btn-primary"
              >{conversion.hero.primaryCta}</a
            >
            <a href="#contact" class="btn btn-outline"
              >{conversion.hero.secondaryCta}</a
            >
            {
              linkedin && (
                <a
                  href={linkedin.href}
                  class="btn btn-ghost"
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  LinkedIn ↗
                </a>
              )
            }
          </div>
          <p class="hero-trust-line">{conversion.hero.trustLine}</p>
        </div>

        <figure class="hero-portrait-wrap hero-card">
          <Picture
            src={portraitImage}
            formats={['avif', 'webp']}
            fallbackFormat="webp"
            widths={heroPortraitWidths}
            sizes={heroPortraitSizes}
            width={800}
            height={1067}
            alt={profile.portrait.alt}
            loading="eager"
            fetchpriority="high"
            decoding="async"
            quality={heroPortraitQuality}
            pictureAttributes={{ class: 'hero-picture' }}
          />
          <figcaption class="hero-portrait-cap">
            {
              t(
                locale,
                'home.portraitCaption',
                '6+ years · React · Next.js · TypeScript · FastAPI',
              )
            }
          </figcaption>
        </figure>

        <div
          class="hero-bottom hero-proof-grid"
          aria-label={t(locale, 'home.heroProofAria', 'Professional proof')}
        >
          {
            heroProofCards.map((card) => (
              <a class={card.className} href={card.href}>
                <span class="hero-card-kicker">{card.eyebrow}</span>
                <strong>{card.title}</strong>
                <span>{card.body}</span>
                {'tags' in card && (
                  <span
                    class="hero-case-tags"
                    aria-label={t(
                      locale,
                      'home.heroCaseTags',
                      'Project focus areas',
                    )}
                  >
                    {card.tags.map((tag) => (
                      <span>{tag}</span>
                    ))}
                  </span>
                )}
                <span class="hero-card-link">{card.cta} →</span>
              </a>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  <!-- ── STATS BAND ────────────────────────────────── -->
  <!-- Static, crawlable proof points; no runtime counter needed. -->
  <aside
    class="stats-band"
    aria-label={t(locale, 'home.statAria', 'Career highlights')}
  >
    <div class="wrap">
      <div class="stats-grid">
        <div class="stat-item reveal">
          <span class="stat-num">6+</span>
          <span class="stat-lbl">
            {t(locale, 'home.statYears', 'Years experience')}
          </span>
        </div>
        <div class="stat-item reveal d1">
          <span class="stat-num">5</span>
          <span class="stat-lbl">
            {t(locale, 'home.statCompanies', 'Companies worked at')}
          </span>
        </div>
        <div class="stat-item reveal d2">
          <span class="stat-num">2</span>
          <span class="stat-lbl">
            {t(locale, 'home.statCapabilities', 'Full-stack capabilities')}
          </span>
        </div>
        <div class="stat-item reveal d3">
          <span class="stat-num">India</span>
          <span class="stat-lbl">
            {t(locale, 'home.statRemote', 'Remote collaboration')}
          </span>
        </div>
      </div>
    </div>
  </aside>

  <HomepageConversion {locale} {work} {pageHref} {caseStudySlugs} />

  <!-- ── FEATURED ARTICLES ─────────────────────────── -->
  <!--
        Cards driven by the blog content collection — adding a post automatically
        adds it here. tags[0] surfaces as the kicker label.
        Section marked with article schema via the BlogPosting JSON-LD on each
        article page; the index links feed crawler discovery.
      -->
  <section
    id="blog"
    class="section section-glow section-glow-tr"
    aria-labelledby="blog-title"
  >
    <div class="wrap">
      <div
        class="section-head"
        style="display:flex;align-items:flex-end;justify-content:space-between;gap:24px;flex-wrap:wrap"
      >
        <div class="reveal">
          <span class="s-eyebrow">06 — {t(locale, 'nav.blog', 'Writing')}</span>
          <h2 id="blog-title" class="s-title">
            {t(locale, 'page.blog.title', 'Featured articles')}
          </h2>
          <p class="s-lead">
            {
              t(
                locale,
                'home.blogLead',
                'Practical guides on performance, accessibility, and modern UI delivery.',
              )
            }
          </p>
        </div>
        <a
          href={pageHref('/blog/')}
          class="btn btn-ghost reveal"
          style="margin-bottom:14px"
          >{t(locale, 'home.allArticles', 'All articles')} →</a
        >
      </div>

      <div class="articles-grid">
        {
          posts.slice(0, 3).map((post, i) => (
            <article
              class={`article-card article-card--visual reveal${i > 0 ? ` d${i}` : ''}`}
            >
              <img
                class="card-illustration"
                src={articleIllustrations[i]}
                alt=""
                aria-hidden="true"
                width="512"
                height="320"
                loading="lazy"
                decoding="async"
              />
              {post.data.tags[0] && (
                <a
                  class="article-kicker"
                  href={getBlogTagHref(locale, post.data.tags[0])}
                  rel="tag"
                >
                  {post.data.tags[0]}
                </a>
              )}
              <h3>{post.data.title}</h3>
              <p>{post.data.description}</p>
              <a
                href={getBlogPostHref(locale, post.id)}
                class="read-link"
                aria-label={`${t(locale, 'home.readArticle', 'Read article')}: ${post.data.title}`}
              >
                {t(locale, 'home.readArticle', 'Read article')}
                <svg
                  width="14"
                  height="14"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2.5"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  aria-hidden="true"
                >
                  <path d="M5 12h14M12 5l7 7-7 7" />
                </svg>
              </a>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ── ABOUT ──────────────────────────────────────── -->
  <!--
        Statement and traits are editorial copy, not pulled from profile.ts —
        they're more specific than the generic `lead` and `about` fields.
        Trait cards use the same accent-line ::before decoration as expertise cards.
      -->
  <section id="about" class="section section-alt" aria-labelledby="about-title">
    <div class="wrap">
      <div class="section-head">
        <span class="s-eyebrow reveal"
          >07 — {t(locale, 'nav.about', 'About')}</span
        >
        <h2 id="about-title" class="s-title reveal">
          {t(locale, 'home.about', 'About me')}
        </h2>
      </div>

      <p class="about-statement reveal">
        {
          t(
            locale,
            'home.aboutStatement',
            'Suprabhat Kumar, also known as Shubh, is a Senior Front-End Engineer with 6+ years building SaaS portals, enterprise dashboards, design systems, and accessible websites. Based in Noida Extension, with delivery experience across teams in Denmark, China, and Australia.',
          )
        }
      </p>

      <div class="about-traits">
        <div class="trait-card trait-card--visual reveal">
          <img
            class="card-illustration"
            src="/assets/illustrations/about-performance.svg"
            alt=""
            aria-hidden="true"
            width="512"
            height="320"
            loading="lazy"
            decoding="async"
          />
          <span class="trait-num">01</span>
          <h3>
            {t(locale, 'trait.performance.title', 'Performance-first')}
          </h3>
          <p>
            {
              t(
                locale,
                'trait.performance.body',
                'I use performance budgets, Core Web Vitals, and measured browser evidence throughout delivery.',
              )
            }
          </p>
        </div>
        <div class="trait-card trait-card--visual reveal d1">
          <img
            class="card-illustration"
            src="/assets/illustrations/about-accessibility.svg"
            alt=""
            aria-hidden="true"
            width="512"
            height="320"
            loading="lazy"
            decoding="async"
          />
          <span class="trait-num">02</span>
          <h3>
            {t(locale, 'trait.accessibility.title', 'Accessibility by default')}
          </h3>
          <p>
            {
              t(
                locale,
                'trait.accessibility.body',
                'Semantic HTML, keyboard navigation, visible focus, and manual assistive-technology review guide implementation.',
              )
            }
          </p>
        </div>
        <div class="trait-card trait-card--visual reveal d2">
          <img
            class="card-illustration"
            src="/assets/illustrations/about-architecture.svg"
            alt=""
            aria-hidden="true"
            width="512"
            height="320"
            loading="lazy"
            decoding="async"
          />
          <span class="trait-num">03</span>
          <h3>
            {t(locale, 'trait.architecture.title', 'Scalable architecture')}
          </h3>
          <p>
            {
              t(
                locale,
                'trait.architecture.body',
                'Reusable component libraries, design tokens, and predictable state management that teams can own long-term.',
              )
            }
          </p>
        </div>
        <div class="trait-card trait-card--visual reveal d3">
          <img
            class="card-illustration"
            src="/assets/illustrations/about-fullstack.svg"
            alt=""
            aria-hidden="true"
            width="512"
            height="320"
            loading="lazy"
            decoding="async"
          />
          <span class="trait-num">04</span>
          <h3>
            {t(locale, 'trait.fullstack.title', 'Full-stack capable')}
          </h3>
          <p>
            {
              t(
                locale,
                'trait.fullstack.body',
                'FastAPI and PostgreSQL support full-stack delivery when a frontend feature needs a focused API or data layer.',
              )
            }
          </p>
        </div>
      </div>

      <p class="hero-ctas reveal">
        <a href="#experience" class="btn btn-outline"
          >{t(locale, 'home.experienceCta', 'Explore experience')} →</a
        >
        <a
          href="/assets/suprabhat-kumar-senior-frontend-engineer-resume.pdf"
          class="btn btn-ghost"
          download
        >
          {t(locale, 'action.resume', 'Download résumé')}
        </a>
      </p>
    </div>
  </section>

  <!-- ── EXPERTISE ────────────────────────────────── -->
  <section
    id="expertise"
    class="section section-glow section-glow-tl"
    aria-labelledby="expertise-title"
  >
    <div class="wrap">
      <div class="section-head reveal">
        <span class="s-eyebrow">
          08 — {t(locale, 'home.expertise', 'Expertise')}
        </span>
        <h2 id="expertise-title" class="s-title">
          {t(locale, 'home.expertise', 'Core expertise')}
        </h2>
        <p class="s-lead">
          {
            t(
              locale,
              'home.expertiseLead',
              'Senior frontend support where architecture, data, accessibility, and performance meet.',
            )
          }
        </p>
      </div>

      <div class="expertise-grid">
        <div class="expertise-card expertise-card--visual mo-reveal">
          <img
            class="card-illustration"
            src="/assets/illustrations/expertise-frontend.svg"
            alt=""
            aria-hidden="true"
            width="512"
            height="320"
            loading="lazy"
            decoding="async"
          />
          <div class="expertise-icon" aria-hidden="true">
            <svg
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              ><polygon points="12 2 22 8.5 22 15.5 12 22 2 15.5 2 8.5 12 2"
              ></polygon><line x1="12" y1="22" x2="12" y2="15.5"
              ></line><polyline points="22 8.5 12 15.5 2 8.5"></polyline></svg
            >
          </div>
          <h3>
            {t(locale, 'expertise.frontend.title', 'Frontend Engineering')}
          </h3>
          <p>
            {
              t(
                locale,
                'expertise.frontend.body',
                'Build SaaS portals and data-heavy dashboards with clear state ownership, resilient data states, accessible interactions, and measured rendering performance.',
              )
            }
          </p>
          <div class="skill-tags">
            <span class="skill-tag">React</span><span class="skill-tag"
              >Next.js</span
            ><span class="skill-tag">TypeScript</span><span class="skill-tag"
              >Redux Toolkit</span
            ><span class="skill-tag">RTK Query</span><span class="skill-tag"
              >TanStack Query</span
            ><span class="skill-tag">Tailwind CSS</span><span class="skill-tag"
              >Ant Design</span
            ><span class="skill-tag">Chakra UI</span><span class="skill-tag"
              >Core Web Vitals</span
            >
          </div>
        </div>

        <div class="expertise-card expertise-card--visual mo-reveal">
          <img
            class="card-illustration"
            src="/assets/illustrations/expertise-backend.svg"
            alt=""
            aria-hidden="true"
            width="512"
            height="320"
            loading="lazy"
            decoding="async"
          />
          <div class="expertise-icon" aria-hidden="true">
            <svg
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              ><ellipse cx="12" cy="5" rx="9" ry="3"></ellipse><path
                d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"></path><path
                d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"></path></svg
            >
          </div>
          <h3>
            {t(locale, 'expertise.backend.title', 'Back-End & API Integration')}
          </h3>
          <p>
            {
              t(
                locale,
                'expertise.backend.body',
                'Integrate REST and GraphQL data flows, or build focused FastAPI and PostgreSQL features when the product needs end-to-end delivery.',
              )
            }
          </p>
          <div class="skill-tags">
            <span class="skill-tag">FastAPI</span><span class="skill-tag"
              >PostgreSQL</span
            ><span class="skill-tag">REST APIs</span><span class="skill-tag"
              >Authentication flows</span
            ><span class="skill-tag">Data pipelines</span><span
              class="skill-tag">GraphQL</span
            ><span class="skill-tag">Node.js</span>
          </div>
        </div>

        <div class="expertise-card expertise-card--visual mo-reveal">
          <img
            class="card-illustration"
            src="/assets/illustrations/expertise-design-systems.svg"
            alt=""
            aria-hidden="true"
            width="512"
            height="320"
            loading="lazy"
            decoding="async"
          />
          <div class="expertise-icon" aria-hidden="true">
            <svg
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              ><path d="M12 2L2 7l10 5 10-5-10-5z"></path><path
                d="M2 17l10 5 10-5"></path><path d="M2 12l10 5 10-5"
              ></path></svg
            >
          </div>
          <h3>
            {
              t(
                locale,
                'expertise.design.title',
                'Design Systems & Performance',
              )
            }
          </h3>
          <p>
            {
              t(
                locale,
                'expertise.design.body',
                'Create component libraries, design tokens, Storybook documentation, accessibility defaults, and adoption patterns teams can extend.',
              )
            }
          </p>
          <div class="skill-tags">
            <span class="skill-tag">Design systems</span><span class="skill-tag"
              >Storybook</span
            ><span class="skill-tag">WCAG 2.2 AA</span><span class="skill-tag"
              >Performance budgets</span
            ><span class="skill-tag">Figma</span><span class="skill-tag"
              >Azure</span
            ><span class="skill-tag">Git</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── SKILLS TABLE ───────────────────────────────── -->
  <section
    id="skills"
    class="section section-alt"
    aria-labelledby="skills-title"
  >
    <div class="wrap">
      <div class="section-head reveal">
        <span class="s-eyebrow">09 — Skills</span>
        <h2 id="skills-title" class="s-title">
          {t(locale, 'page.skills.eyebrow', 'Skills')}
        </h2>
        <p class="s-lead">
          {
            t(
              locale,
              'home.skillsLead',
              'The technical stack I use to deliver scalable, performant, accessible UI systems.',
            )
          }
        </p>
      </div>
      <div class="skills-table">
        <div class="skills-row">
          {
            skillColumns.map((column) => (
              <div class="skill-col mo-reveal">
                <h3>{column.heading}</h3>
                <ul class="skill-ul">
                  {column.items.map((item) => (
                    <li>{item}</li>
                  ))}
                </ul>
              </div>
            ))
          }
        </div>
      </div>
      <p style="margin-top:28px" class="reveal d1">
        <a href="#contact" class="btn btn-outline"
          >{t(locale, 'home.skillsCta', 'Discuss a technical challenge')} →</a
        >
      </p>
    </div>
  </section>

  <!-- ── EXPERIENCE TIMELINE ───────────────────────── -->
  <!-- Fully driven by experience.ts — add/edit a job there, HTML updates automatically -->
  <section
    id="experience"
    class="section section-glow section-glow-br"
    aria-labelledby="exp-title"
  >
    <div class="wrap">
      <div class="section-head reveal">
        <span class="s-eyebrow">
          10 — {t(locale, 'home.experience', 'Experience')}
        </span>
        <h2 id="exp-title" class="s-title">
          {t(locale, 'home.experience', 'Experience')}
        </h2>
      </div>
      <div class="timeline-wrap">
        <!-- CSS-drawn timeline rail; no runtime animation required. -->
        <div class="tl-line" id="tl-line" aria-hidden="true"></div>
        <ol
          class="exp-list"
          aria-label={t(locale, 'home.history', 'Work history, newest first')}
        >
          {
            experience.map((job, idx) => (
              <li class={`exp-entry${idx === 0 ? ' current' : ''} mo-reveal`}>
                <div class="dot-col" aria-hidden="true">
                  <div class="exp-dot" />
                </div>
                <div class="exp-body">
                  <div class="exp-head">
                    <div>
                      <p class="exp-company">{job.company}</p>
                      {job.title && (
                        <h3 class="exp-title-h3">
                          {t(locale, `experience.${job.id}.title`, job.title)}
                        </h3>
                      )}
                      {!job.title && job.roles && (
                        <h3 class="exp-title-h3">
                          {job.roles
                            .map((role) =>
                              t(
                                locale,
                                `experience.${job.id}.${role.promotion ? 'senior' : 'developer'}`,
                                role.title,
                              ),
                            )
                            .join(' → ')}
                        </h3>
                      )}
                    </div>
                    <time class="exp-date" datetime={toISO(job.start)}>
                      {job.startLabel ?? job.start} — {job.endLabel ?? job.end}
                      {job.mode && ` · ${job.modeLabel ?? job.mode}`}
                      {job.location &&
                        ` · ${job.locationLabel ?? job.location}`}
                      {job.duration &&
                        ` · ${job.durationLabel ?? job.duration}`}
                    </time>
                  </div>
                  {job.bullets && (
                    <ul class="exp-bullets">
                      {job.bullets.map((bullet, index) => (
                        <li>
                          {t(
                            locale,
                            `experience.${job.id}.b${index + 1}`,
                            bullet,
                          )}
                        </li>
                      ))}
                    </ul>
                  )}
                  {job.roles && (
                    <div class="sub-roles">
                      {job.roles.map((role) => (
                        <div>
                          <span class="sub-role-meta">
                            {role.startLabel ?? role.start} —{' '}
                            {role.endLabel ?? role.end}
                          </span>
                          <h4 class="sub-role-h4">
                            {t(
                              locale,
                              `experience.${job.id}.${role.promotion ? 'senior' : 'developer'}`,
                              role.title,
                            )}
                            {role.promotion && (
                              <span class="promo-badge">
                                {t(locale, 'home.promoted', 'Promoted')}
                              </span>
                            )}
                          </h4>
                          <ul class="exp-bullets">
                            {role.bullets.map((bullet, index) => (
                              <li>
                                {t(
                                  locale,
                                  `experience.${job.id}.${role.promotion ? 'senior' : 'developer'}.b${index + 1}`,
                                  bullet,
                                )}
                              </li>
                            ))}
                          </ul>
                        </div>
                      ))}
                    </div>
                  )}
                </div>
              </li>
            ))
          }
        </ol>
      </div>
    </div>
  </section>

  <!-- ── TOOLS GRID ───────────────────────────────────── -->
  <section
    id="tools"
    class="section section-alt cv-auto"
    aria-labelledby="tools-title"
  >
    <div class="wrap">
      <div class="section-head reveal">
        <span class="s-eyebrow">
          11 — {t(locale, 'home.toolbox', 'Stack')}
        </span>
        <h2 id="tools-title" class="s-title">
          {t(locale, 'page.tools.title', 'Tools & technologies')}
        </h2>
        <p class="s-lead">
          {
            t(
              locale,
              'home.stackLead',
              'The stack I use to ship fast, accessible, scalable products.',
            )
          }
        </p>
      </div>
    </div>

    <div
      class="tm-wrap mo-reveal"
      aria-label={t(locale, 'home.stackAria', 'Technology stack')}
    >
      <ul class="tm-grid" role="list">
        {
          tools.map((tool) => (
            <li class="tm-item">
              <div class="tm-chip">
                <span class="tm-cat">{toolCategoryLabel(tool.name)}</span>
                <img
                  src={tool.icon}
                  alt=""
                  width="36"
                  height="36"
                  loading="lazy"
                  decoding="async"
                  class:list={[{ 'tm-invert': tool.invertOnDark }]}
                />
                <span class="tm-name">{tool.name}</span>
              </div>
            </li>
          ))
        }
      </ul>
    </div>
  </section>

  {
    featured.length > 0 && (
      <section
        id="recommendations"
        class="section section-alt cv-auto"
        aria-labelledby="recs-title"
      >
        <div class="wrap">
          <div class="section-head reveal">
            <span class="s-eyebrow">
              12 — {t(locale, 'home.recommendations', 'Recommendations')}
            </span>
            <h2 id="recs-title" class="s-title">
              {t(locale, 'home.recommendations', 'Recommendations')}
            </h2>
            <p class="s-lead">
              {t(
                locale,
                'home.recommendationsLead',
                'What cross-functional partners say about working together.',
              )}
            </p>
          </div>
          <div class="recs-grid">
            {featured.map((item) => (
              <figure class="rec-card mo-reveal">
                <div class="rec-quote-mark" aria-hidden="true">
                  "
                </div>
                <blockquote
                  lang={item.quoteLang}
                  dir={item.quoteLang === 'en' ? 'ltr' : undefined}
                >
                  "{item.quote}"
                </blockquote>
                <figcaption class="rec-author">
                  <img
                    src={item.avatar}
                    alt=""
                    width="44"
                    height="44"
                    loading="lazy"
                    decoding="async"
                  />
                  <div>
                    <span class="rec-name">{item.name}</span>
                    <span class="rec-role">{item.role}</span>
                  </div>
                </figcaption>
              </figure>
            ))}
          </div>
          <p style="margin-top:28px" class="reveal">
            <a href={pageHref('/recommendations/')} class="btn btn-ghost">
              {t(
                locale,
                'home.recommendationsCta',
                'View verified recommendations',
              )}{' '}
              →
            </a>
          </p>
        </div>
      </section>
    )
  }

  <!-- ── FAQ ───────────────────────────────────────── -->
  <!--
        Native <details>/<summary> handles open/close with zero JS.
        faq.answer drives both the visible text and the FAQPage JSON-LD route
        metadata — single source of truth, they cannot drift.
      -->
  <section
    id="faq"
    class="section cv-auto section-glow section-glow-bl"
    aria-labelledby="faq-title"
  >
    <div class="wrap">
      <div class="faq-2col">
        <div class="reveal">
          <span class="s-eyebrow"
            >{featured.length > 0 ? '13' : '12'} — FAQ</span
          >
          <h2 id="faq-title" class="s-title">
            {t(locale, 'page.faq.title', 'Frequently asked questions')}
          </h2>
          <p class="s-lead" style="margin-bottom:28px">
            {
              t(
                locale,
                'home.faqLead',
                'Quick answers about services, pricing, and how we can collaborate.',
              )
            }
          </p>
          <a href="#contact" class="btn btn-primary"
            >{t(locale, 'action.start', 'Start a project')}</a
          >
        </div>
        <div class="reveal d1">
          <ul class="faq-list" role="list">
            {
              faqs.map((faq) => (
                <li>
                  <details>
                    <summary>
                      {t(locale, `faq.${faq.id}.q`, faq.question)}
                      <span class="faq-icon" aria-hidden="true">
                        +
                      </span>
                    </summary>
                    <div class="faq-body">
                      {t(locale, `faq.${faq.id}.a`, faq.answer)}
                    </div>
                  </details>
                </li>
              ))
            }
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- \u2500\u2500 CTA BAND \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 -->
  <section aria-labelledby="cta-title" class="cta-band">
    <div class="cta-glow-l" aria-hidden="true"></div>
    <div class="cta-glow-r" aria-hidden="true"></div>
    <div class="wrap cta-inner">
      <div class="reveal">
        <p class="cta-tag">
          {t(locale, 'home.ctaTag', 'Ready to collaborate')}
        </p>
        <h2 id="cta-title" class="cta-title">
          {
            t(
              locale,
              'home.ctaTitle',
              'Bring the frontend problem. Get a defined path forward.',
            )
          }
        </h2>
        <p class="cta-sub">
          {
            t(
              locale,
              'home.ctaLead',
              'Share the product context, timeline, and constraints. I will review the fit and propose the next step.',
            )
          }
        </p>
      </div>
      <div class="cta-btns reveal d1">
        <a href="#contact" class="btn btn-primary"
          >{t(locale, 'action.start', 'Start a project')}</a
        >
        <a href={pageHref('/blog/')} class="btn btn-outline"
          >{t(locale, 'nav.blog', 'Read the blog')}</a
        >
      </div>
    </div>
  </section>

  <!-- \u2500\u2500 CONTACT \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 -->
  <section
    id="contact"
    class="section cv-auto section-glow section-glow-tl"
    aria-labelledby="contact-title"
  >
    <div class="wrap">
      <div class="section-head reveal">
        <span class="s-eyebrow">
          {featured.length > 0 ? '14' : '13'} — {
            t(locale, 'page.contact.eyebrow', 'Contact')
          }
        </span>
        <h2 id="contact-title" class="s-title">
          {t(locale, 'page.contact.eyebrow', 'Contact')}
        </h2>
        <p class="s-lead">
          {
            t(
              locale,
              'home.contactLead',
              'Need senior frontend support for a dashboard, architecture review, design system, audit, or website? Send the context below.',
            )
          }
        </p>
      </div>
      <div class="contact-2col">
        <div class="reveal">
          <nav aria-label={t(locale, 'home.contactOptions', 'Contact options')}>
            <ul class="contact-links-list">
              <li>
                <a href={`mailto:${profile.email}`} class="c-link">
                  <span class="c-icon" aria-hidden="true">
                    <svg
                      width="18"
                      height="18"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      ><rect x="2" y="4" width="20" height="16" rx="2"
                      ></rect><path d="M22 4l-10 8L2 4"></path></svg
                    >
                  </span>
                  <div class="c-text">
                    <strong>{t(locale, 'home.email', 'Email')}</strong><span
                      >{profile.email}</span
                    >
                  </div>
                </a>
              </li>
              {
                profile.social
                  .filter((s) => s.label === 'LinkedIn')
                  .map((s) => (
                    <li>
                      <a
                        href={s.href}
                        class="c-link"
                        target="_blank"
                        rel="noopener noreferrer"
                      >
                        <span class="c-icon" aria-hidden="true">
                          <svg
                            width="18"
                            height="18"
                            viewBox="0 0 24 24"
                            fill="currentColor"
                          >
                            <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z" />
                          </svg>
                        </span>
                        <div class="c-text">
                          <>
                            <strong>LinkedIn</strong>
                            <span>{s.href.replace('https://', '')}</span>
                          </>
                        </div>
                      </a>
                    </li>
                  ))
              }
              {
                profile.social
                  .filter((s) => s.label === 'GitHub')
                  .map((s) => (
                    <li>
                      <a
                        href={s.href}
                        class="c-link"
                        target="_blank"
                        rel="noopener noreferrer"
                      >
                        <span class="c-icon" aria-hidden="true">
                          <svg
                            width="18"
                            height="18"
                            viewBox="0 0 24 24"
                            fill="currentColor"
                          >
                            <path d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0112 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z" />
                          </svg>
                        </span>
                        <div class="c-text">
                          <>
                            <strong>GitHub</strong>
                            <span>{s.href.replace('https://', '')}</span>
                          </>
                        </div>
                      </a>
                    </li>
                  ))
              }
            </ul>
          </nav>
        </div>

        <!-- Web3Forms \u2014 access key injected at build time from PUBLIC_WEB3FORMS_KEY env var -->
        <ContactForm {locale} variant="homepage" />
      </div>
    </div>
  </section>
</div>

<!-- Back-to-top; hidden attr toggled by scroll JS below -->
<button
  type="button"
  class="btt"
  id="btt"
  aria-label={t(locale, 'action.top', 'Back to top')}
  hidden
>
  <svg
    width="20"
    height="20"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2.5"
    stroke-linecap="round"
    stroke-linejoin="round"
    aria-hidden="true"
  >
    <path d="M18 15l-6-6-6 6"></path>
  </svg>
</button>

<!--
      Homepage client behaviours - functional only, no decorative runtime motion.
      Astro defers all <script> modules automatically.
      Covers: back-to-top.
    -->
<script>
  (function () {
    'use strict';

    // Back to top.
    const btt = document.getElementById('btt') as HTMLButtonElement | null;
    if (btt) {
      window.addEventListener(
        'scroll',
        function () {
          btt.hidden = window.scrollY < 500;
        },
        { passive: true },
      );
      btt.addEventListener('click', function () {
        const behavior = window.matchMedia('(prefers-reduced-motion: reduce)')
          .matches
          ? 'auto'
          : 'smooth';
        window.scrollTo({ top: 0, behavior });
      });
    }
  })();
</script>
